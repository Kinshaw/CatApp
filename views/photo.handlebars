<section class="photo">
  <img src="/images/{{photo.filename}}" alt="{{photo.description}}">
  <div class="photo-details">
    <h3>{{photo.title}}</h3>
    <p>By: {{photo.artist}}</p>
    <p>Exhibition open until: {{ format_date photo.exhibition_date}}</p>
  </div>
</section>
